<% if params[:action] == 'edit' || params[:action] == 'update' %>

  <%= f.submit "郵便番号で検索", class: 'btn btn-default col-xs-2', data: {confirm: "新たに郵便番号から検索しますか？"} %>

<% else %>

  <%= f.submit "郵便番号で検索", class: 'btn btn-default col-xs-2' %>
<% end %>


